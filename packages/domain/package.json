{
  "name": "@foresift/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure TypeScript domain contracts for Foresift: identity, time, quality, acquisition, lineage, recovery. Zero runtime dependencies.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "bun test --no-orphans --isolate --parallel=1 --max-concurrency=1 --timeout=30000"
  },
  "devDependencies": {}
}
