{
  "name": "@foresift/providers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Foresift audited read-only provider adapter framework bound to exact per-adapter egress allowlists, plus the safety-critical reference families: GMGN strictly query-only and Helius supported raw/history operations with local supported-program decoding separated from deprecated provider parsing.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@foresift/domain": "workspace:*",
    "@foresift/provider-lifecycle": "workspace:*",
    "@foresift/security": "workspace:*",
    "@foresift/shared-schemas": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.6",
    "vitest": "4.1.11"
  }
}
