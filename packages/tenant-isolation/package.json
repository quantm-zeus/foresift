{
  "name": "@foresift/tenant-isolation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Foresift tenant-isolation primitives: tenant context derivation, namespaced keys for every shared surface (cache, queues, sessions, quotas, logs, metrics, model context), fail-closed row scoping, tenant-bound signed URLs, and cross-tenant resource-access authorization.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "@foresift/domain": "workspace:*",
    "@foresift/security": "workspace:*",
    "@foresift/shared-schemas": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.6"
  }
}
