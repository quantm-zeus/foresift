# Tasks: g0-cost-capacity

**Input**: `specs/g0-cost-capacity/spec.md` (FR-COST-001…010), `specs/g0-cost-capacity/plan.md`, capsule `plan-capsule.md` bound to main `b4978cdbf332` / manifest `e0f9f1284473` / PRD `baa521d9c67e`
**Prerequisites**: plan.md (required), spec.md (required), no ahead-of-plan implementation beyond PROVEN dependencies
**Tests**: Every AC has a positive AND a negative/failure suite at the manifest-declared paths `tests/acceptance/AC-*.spec.ts` / `tests/negative/AC-*.negative.spec.ts`
**Organization**: Tasks are grouped by dependency order — setup → schema/migrations → cost-router core → capacity-planner → quota-forecast → integration → acceptance — so later phases can consume earlier contracts without circular blocking. Parallelizable tasks are marked `[P]`.
**Traceability**: Every line traces to at least one requirement ID or acceptance criterion from this package. No out-of-scope requirement IDs appear below.

---

## Phase 0: Setup

- [x] T001 Scaffold the three new workspace packages with pnpm entries and configs — create `packages/cost-router/**`, `packages/capacity-planner/**`, `packages/quota-forecast/**` each with `package.json` (`@foresift/cost-router` et al., `"test": "bun test"`) and `tsconfig.json` (extends `../../tsconfig.base.json`, globs `src/**` + `test/**`); no extra test runner config — the repository test authority is Bun Test and existing packages carry no runner config files; verify `pnpm install` still resolves and glob picks them up with zero root-config edits (FR-COST-001…010 infrastructure) — **FR-COST-001**
- [ ] T002 [P] Create `tests/fixtures/cost/` fixture directory with provider operation snapshots (free-unmetered, free-quota, paid, unknown-cost), batch inputs, forecast plan fixtures, and paid-policy fixtures used by later phases (FR-COST-001, FR-COST-002, FR-COST-005, FR-COST-008) — **FR-COST-001**
- [x] T003 [P] Create `telemetry/cost.catalog.json` declarative event-contract catalog (events: `cost.blocked`, `cost.quota_committed`, `cost.quota_released`, `cost.quota_expired`, `cost.reserve_consumed`, `cost.batch_coalesced`, `cost.forecast_computed`, `cost.plan_unverified`, `cost.paid_policy_activated`, `cost.capacity_replay_blocked`, `cost.resource_budget_exhausted`, `cost.forecast_tolerance_breached`; fields mirror `packages/shared-schemas/src/cost.ts`; emitter wiring deferred to G2) (FR-COST-001…010 catalog) — **FR-COST-007**

---

## Phase 1: Domain vocabularies + shared schemas (blocking prerequisite)

- [x] T004 Extend the shared domain vocabulary package (packages/domain) with `packages/domain/src/cost.ts` — pure enums/helpers `CostClass`, `QuotaModel` (reuse existing), `ResetPolicyKind`, `ReserveId` (RISK_MONITORING | ALERT_VERIFICATION | INTERACTIVE_MCP | EMERGENCY_BACKFILL), `ResourceBudgetKind` (SCHEDULER_SLOTS | WORKFLOW_STEPS | DATABASE_BYTES | OBJECT_STORE_BYTES | NOTIFICATION_RATE | MODEL_TOKENS_BYOK), `CostMode` (STRICT_FREE | PAID_ENABLED) with fail-closed parse helpers (`costClass(v)` etc. throwing `ForesiftError` with stable codes); export from `packages/domain/src/index.ts` (FR-COST-001) — **FR-COST-001**
- [x] T005 Implement `packages/shared-schemas/src/cost.ts` authoritative Zod schemas (ADR-0013) — `OperationCostDeclarationSchema`, `CostModeSchema`, `QuotaBalanceSchema`, `ReserveBucketSchema`, `PaidProviderPolicySchema`, `ResourceBudgetSchema`, `ForecastSnapshotSchema`, `CostDenialRecordSchema`, `BatchDescriptorSchema`, `BatchCoalescedEventSchema` plus catalog field parity test; bump `COST_SCHEMA_REGISTRY_VERSION` only on breaking change; export from `packages/shared-schemas/src/index.ts` (FR-COST-001) — **FR-COST-001**
- [ ] T006 Unit tests — domain parse helpers fail-closed on every unknown enum value; shared-schema strictness rejects unknown keys and missing required fields (`packages/shared-schemas/test/cost.spec.ts`, `packages/domain/test/cost.spec.ts`) — tests prove fail-closed shapes (FR-COST-001) — **FR-COST-001**

---

## Phase 2: Migrations

- [x] T007 Write `migrations/g0_cost_0001_cost_ledgers.sql` — `cost_quota_balances` (pk providerId, quotaModelId, periodWindowStart) + `cost_reserve_buckets` (pk reserveId, providerId, periodWindowStart) + `cost_usage_counters` with `remainingUnits = capLimit − consumedReserved − consumedCommitted` CHECK, reserve FK, no-reserve-invasion constraint; single-transaction DDL with rollback header; runner-ordered with existing families (FR-COST-001, FR-COST-002, FR-COST-003) — **FR-COST-001** **FR-COST-002** **FR-COST-003**
- [x] T008 Write `migrations/g0_cost_0002_paid_policies.sql` — `paid_provider_policies` (policyId hash, providerId, budgetUnits, approvedBy, approvedAt, activatedAt, reAuthDueAt, active, supersededBy); partial unique index on active=true per provider; `reAuthDueAt > activatedAt` CHECK; immutability invariant (no UPDATE of budget/approver once active — trigger or revoked grant, asserted by `migrations.spec.ts`) (FR-COST-008) — **FR-COST-008**
- [x] T009 Write `migrations/g0_cost_0003_capacity_budgets.sql` — `capacity_resource_budgets` (kind, capLimit, used, forecastUsed, degradeBehavior, ceilingExceededAt); enum CHECK over six ResourceBudgetKinds; `used ≤ capLimit`; BYOK kind isolated (FR-COST-009) — **FR-COST-009**
- [x] T010 Write `migrations/g0_cost_0004_resource_forecast_snapshots.sql` — `resource_forecast_snapshots` (snapshotId, planVersionId, verifiedAt, expiresAt, planLimitsJson, observedUsageJson, estimatedForecastJson) + `capacity_replay_runs` (replayId, snapshotId, mode expected|stress, blockingFlag, incidentId); expiry-indexed; FK from replay to snapshot (FR-COST-006) — **FR-COST-006**
- [ ] T011 [P] Colocated `migrations.spec.ts` in each package — assert migration filenames known, ordering enforced, checksum-pinned, apply-on-PGlite idempotent via the packages/persistence test engine per ADR-0014; ALSO update the central migration registry `packages/persistence/test/migrator.spec.ts` so its expected G0 script list includes the four `g0_cost_` scripts in lexicographic position (plan-sanctioned scope exception, mirroring the g0-provider-lifecycle precedent — the central suite asserts EXACTLY the full G0 script set, so every new family must extend it) (FR-COST-001…010 substrate) — **FR-COST-001**

---

## Phase 3: Cost-router core — declarations, STRICT_FREE, reserves, batch, quota adapter

- [x] T012 Implement `packages/cost-router/src/cost-declaration.ts` — read view over `prov.prov_operations` projecting the 7 FR-COST-001 fields (`costClass`, `quotaUnitCost`, `resetPolicyId`, `batchCapability`, `minimumCandidateStage`, `protectedReserveEligible`, `allowedInStrictFree`), fail-closed when any field is absent or holds an unrecognized enum; rejection code `UNKNOWN_COST`; unit `cost-declaration.spec.ts` proves every gap refuses (FR-COST-001) — **FR-COST-001** — AC-100, AC-103
- [x] T013 Implement `packages/cost-router/src/cost-mode.ts` — `CostMode` policy holder (`STRICT_FREE` default constant, query for active PAID_ENABLED paid policies via paid-policy module), per-call mode resolution; wiring point for the composition root that exposes `LicensePolicySource` for tool-core's cache-key license version component (FR-COST-008, FR-COST-010) — **FR-COST-008** **FR-COST-010**
- [x] T014 Implement `packages/cost-router/src/strict-free-guard.ts` — deny-closed predicate for STRICT_FREE: blocks `PAID_EXPLICIT`, `UNKNOWN_COST`, over-quota `QUOTA_EXHAUSTED`, auto-upgrade paths, and paid-fallback before any egress; returns typed `CostDenial` (candidate, caller, reason with machine prefix `STRICT_FREE_BLOCKED:*`/`PAID_BLOCKED:*`/`UNKNOWN_COST:*`/`AUTO_UPGRADE_BLOCKED:*`/`PAID_FALLBACK_BLOCKED:*`, alternative); negative tests assert each branch refuses (FR-COST-002) — **FR-COST-002** — AC-100
- [x] T015 Implement `packages/cost-router/src/reserve-router.ts` — protected-reserve routing for the four named reserves (RISK_MONITORING, ALERT_VERIFICATION, INTERACTIVE_MCP, EMERGENCY_BACKFILL); maps `(workloadClass, operation)` → `reserveId|null`; refuses to route broad-scan workloads (BACKFILL_LOW/EVALUATION_LOW/SCHEDULED_NORMAL) to any reserve; enforces degrade-breadth/depth before protected quota (FR-COST-003, FR-COST-004) — **FR-COST-003** **FR-COST-004** — AC-101, AC-228
- [x] T016 Implement `packages/cost-router/src/batch-coalescer.ts` — batch coalescing governed by `batchCapability` (maxBatchSize, safe max utilization, keyFields); groups compatible requests inside the coalescing window, emits one provider call per batch, returns batch utilization for telemetry; bit-for-bit deterministic key construction; error cases never merge across providers (FR-COST-005) — **FR-COST-005** — AC-102
- [x] T017 Implement `packages/cost-router/src/cost-audit.ts` — shapes FR-COST-007 denial payloads (`candidate`, `caller`, `reason`, `alternative`) delivered to tool-core stage 23; typed fields mirror `packages/shared-schemas/src/cost.ts`'s `CostDenialRecordSchema`; never emits secrets (FR-COST-007) — **FR-COST-007** — AC-100
- [x] T018 Implement `packages/cost-router/src/quota-adapter.ts` — `QuotaReservationAdapter` implementation (the PRIMARY composition-root seam) implementing `estimate / admit / reserve / commit / release` by composing cost-declaration, cost-mode, strict-free guard, reserve-router, batch coalescer, quota ledgers, and paid-policy; preserves the guarded SQL idempotency semantics and stages 6–11 ahead-of-reservation ordering already frozen in the packages/tool-core package; emits one reservation per provider call, not per batched item; in STRICT_FREE blocks paid-fallback attempts (FR-COST-001…005, FR-COST-008) — **FR-COST-001** **FR-COST-002** **FR-COST-005** — AC-100, AC-102
- [x] T019 Implement `packages/cost-router/src/paid-policy.ts` — immutable `paid_provider_policies` lifecycle: `create(policy)` (requires budget, approver, initial expiry), `activate(policyId, approver)`, `reAuthenticate(policyId, approver)`, supersession via `supersededBy`; ACTIVE uniqueness per provider; `reAuthDueAt` expiry renders call inadmissible until re-auth; update-of-budget/approver after activation refused both at API and SQL layer; BYOK model budget is not consulted (FR-COST-008, FR-COST-010) — **FR-COST-008** **FR-COST-010** — AC-105
- [ ] T020 Colocated unit suites `packages/cost-router/test/**` — `cost-declaration.spec.ts` (unknown enum fails), `strict-free-guard.spec.ts` (every block prefix), `reserve-router.spec.ts` (reserve eligibility + broad-scan refusal), `batch-coalescer.spec.ts` (max utilization + cross-provider never merges), `quota-adapter.spec.ts` (estimate/admit/reserve/commit/release state-machine congruence with `quota-contract.ts` helpers), `paid-policy.spec.ts` (immutability + re-auth expiry), plus `migrations.spec.ts` ordering/pinning (FR-COST-001…010) — **FR-COST-001** **FR-COST-002**

---

## Phase 4: Capacity-planner — degrade policy, resource budgets, composition

- [x] T021 Implement `packages/capacity-planner/src/degrade-policy.ts` — broad-scan degrade strategy: on general-pool exhaustion, degrade breadth (fewer candidates) then depth (fewer fields/shallower history) before touching protected reserves; deterministic ordering; workload priority tie-break matches AC-228's stated order (social/analog/wallet-history/exploration/broad-scan depth degrade first) (FR-COST-004) — **FR-COST-004** — AC-101, AC-228
- [x] T022 Implement `packages/capacity-planner/src/backpressure-mapper.ts` — maps capacity-planner decisions to `BackpressurePolicy` verbs (`RETURN_CACHE`, `DOWNGRADE_DEPTH`, `SKIP_LOW_PRIORITY`, `QUOTA_EXHAUSTED`) consumed by tool-core stage 12; mapping from the defaultBackpressurePolicy contract defined in packages/tool-core/src/stages/quota.ts (read-only reference) (FR-COST-004) — **FR-COST-004**
- [x] T023 Implement `packages/capacity-planner/src/resource-budgets.ts` — six independently capped dimensions (scheduler/workflow/db/object-store/notification/MODEL_TOKENS_BYOK); per-budget cap/used/forecastUsed/degradeBehavior; exhausting one degrades only its dependent workload, never frozen evidence or critical risk monitoring; BYOK model tokens are a separate namespace from every `cost_quota_balances` counter (FR-COST-009, FR-COST-010) — **FR-COST-009** **FR-COST-010** — AC-104, AC-105
- [x] T024 Implement `packages/capacity-planner/src/planner.ts` — composition that merges `cost-router` admission + `resource-budgets` into the single `QuotaAdmissionDecision` seen by tool-core stage 12; data-provider STRICT_FREE and BYOK model budget are disjoint adapter bindings that never share state (FR-COST-004, FR-COST-009) — **FR-COST-004** **FR-COST-009** — AC-101, AC-104, AC-228
- [ ] T025 Colocated unit suites `packages/capacity-planner/test/**` — `degrade-policy.spec.ts` (ordered degrade before reserve; deterministic), `resource-budgets.spec.ts` (per-budget cap independence + BYOK isolation), `planner.spec.ts` (merged decision precedence), `migrations.spec.ts` (FR-COST-004, FR-COST-009) — **FR-COST-004** **FR-COST-009**

---

## Phase 5: Quota-forecast — verified plan freshness, usage ledger, forecast, capacity replay

- [x] T026 Implement `packages/quota-forecast/src/plan-verifier.ts` — watches `verificationExpiresAt` / `resource_forecast_snapshots.expiresAt`; when a snapshot is stale, dependent operations become `UNVERIFIED` and `estimate` throws rather than assuming stale limits; re-verification (fresh documentation/pricing/rights TTL from provider-lifecycle) clears UNVERIFIED; one-second before/after expiry is deterministically correct (FR-COST-006) — **FR-COST-006** — AC-103
- [x] T027 Implement `packages/quota-forecast/src/usage-ledger.ts` — observed-usage counters per `(providerId, quotaModelId, periodWindow)` — the actuals substrate for forecast and capacity replay; persisted in `cost_usage_counters`; surfaced for 30-day replay inputs (FR-COST-006) — **FR-COST-006**
- [x] T028 Implement `packages/quota-forecast/src/forecast.ts` — `CostForecast` = current verified `planLimitsJson` × `observedUsageJson` → `estimatedForecast` vs `actualObserved` vs `delta` vs `withinTolerance` boolean; when tolerance breached, raises incident and recomputes admission caps without silently spending paid overage or reserve (FR-COST-006) — **FR-COST-006** — AC-229
- [x] T029 Implement `packages/quota-forecast/src/capacity-replay.ts` — 30-day expected + stress replay: enumerates credits/rates, streamed bytes, model tokens, workflow steps, DB/object growth, egress, retries, notifications, reserves (FR-COST-009 + AC class); activation blocking when any verified ceiling exceeded; low-priority degradation order asserted against exhaustion replay; determinism snapshot (same inputs → same replay) (FR-COST-006, FR-COST-009) — **FR-COST-006** **FR-COST-009** — AC-227, AC-228, AC-229
- [ ] T030 Colocated unit suites `packages/quota-forecast/test/**` — `plan-verifier.spec.ts` (TTL edge: 1 s before/after), `forecast.spec.ts` (arithmetic + tolerance breach → incident), `capacity-replay.spec.ts` (deterministic 30-day replay + blocking flag), `migrations.spec.ts` (FR-COST-006, FR-COST-009) — **FR-COST-006** **FR-COST-009**

---

## Phase 6: Composition root wiring + contract probes

- [x] T031 Wire the three packages into a single composition root used by tests/ACs — construct a `QuotaReservationAdapter` + `LicensePolicySource` bound to PGlite + provider-operation fixture registry + plan snapshots that the packages/tool-core package's `makeQuotaEstimateStage`/`makeQuotaReserveStage`/`LicensePolicySource` composition points accept; prove unbound adapter is itself deny-closed (no seam → no estimate → no reservation) (FR-COST-001…010 seam) — **FR-COST-001** **FR-COST-002**
- [ ] T032 Cross-package contract probe — run tool-core stage 12→13→18 through the cost-router adapter on a synthetic free-quota operation and assert: fresh estimate → admitted → one reservation row in `core.core_quota_reservations` with matching ledger debit; commit/release paths restore/debit correctly; replayed `reserve(pipelineRunId, stage)` is idempotent (FR-COST-001…005 seam preservation) — **FR-COST-001** **FR-COST-005**

---

## Phase 7: Acceptance + negative suites (12 ACs — each AC has exactly two files)

Header comment in every file states the requirement IDs the file traces and the package-owned facet ("cost facet: …") vs collector/platform substrate. AC-224/225/226 are tagged `(cost facet — collector data-truth substrate owned elsewhere)`. All suites run on PGlite via `@foresift/persistence` test engine.

- [ ] T033 AC-100 suite pair — `tests/acceptance/AC-100.spec.ts` + `tests/negative/AC-100.negative.spec.ts`: in `STRICT_FREE`, attempted paid / unknown-cost / overage / auto-upgrade / paid-fallback calls are blocked before network execution (pipeline-stage ordering assert) and audited with candidate/caller/reason/alternative; negative mutates one field/mode and asserts still blocked with audit entry on replay (FR-COST-001, FR-COST-002, FR-COST-007) — **FR-COST-002** **FR-COST-007**
- [ ] T034 AC-101 suite pair — `tests/acceptance/AC-101.spec.ts` + `tests/negative/AC-101.negative.spec.ts`: broad discovery exhaustion → reduced scan breadth or cached return without consuming protected reserves (assert all four reserve counters unchanged) (FR-COST-003, FR-COST-004) — **FR-COST-003** **FR-COST-004**
- [ ] T035 AC-102 suite pair — `tests/acceptance/AC-102.spec.ts` + `tests/negative/AC-102.negative.spec.ts`: compatible batch of token market requests produces configured maximum safe batch utilization and exactly one reservation per provider call; negative: ungroupable/incompatible requests produce separate reservations (FR-COST-005) — **FR-COST-005**
- [ ] T036 AC-103 suite pair — `tests/acceptance/AC-103.spec.ts` + `tests/negative/AC-103.negative.spec.ts`: provider plan metadata becoming unverified → operations transition to `UNVERIFIED`/`COST_BLOCKED` instead of assuming old free limits; re-verification clears (FR-COST-006) — **FR-COST-006**
- [ ] T037 AC-104 suite pair — `tests/acceptance/AC-104.spec.ts` + `tests/negative/AC-104.negative.spec.ts`: exhausting low-priority scheduler/storage/model budgets degrades enrichment/retention per policy without deleting frozen evidence or stopping risk monitoring (FR-COST-009) — **FR-COST-009**
- [ ] T038 AC-105 suite pair — `tests/acceptance/AC-105.spec.ts` + `tests/negative/AC-105.negative.spec.ts`: nonzero approved BYOK model budget runs the headless agent while data-provider mode stays `STRICT_FREE` with zero paid data calls; negative: data-provider paid attempt still blocks (FR-COST-010, FR-COST-008) — **FR-COST-010** **FR-COST-008**
- [ ] T039 AC-224 suite pair — `tests/acceptance/AC-224.spec.ts` + `tests/negative/AC-224.negative.spec.ts` (cost facet: collector reconnect/ checkpoint resume does not duplicate quota reservations or evidence linked to cost admission; gap detection's cost accounting is consistent through resume/replay) — **FR-COST-001** **FR-COST-005**
- [ ] T040 AC-225 suite pair — `tests/acceptance/AC-225.spec.ts` + `tests/negative/AC-225.negative.spec.ts` (cost facet: backfilled event's cost impact is scoped to its retrieval-time ledger window; historical replay before retrieval sees no cost impact from the backfilled window) — **FR-COST-001**
- [ ] T041 AC-226 suite pair — `tests/acceptance/AC-226.spec.ts` + `tests/negative/AC-226.negative.spec.ts` (cost facet: first-seen latency decomposition spans computed through the cost-admission path include provider comparison alternative; cost-path latency not inflated by reserve routing) — **FR-COST-006**
- [ ] T042 AC-227 suite pair — `tests/acceptance/AC-227.spec.ts` + `tests/negative/AC-227.negative.spec.ts`: 30-day expected + stress capacity replay includes all nine dimension families (credits/rates, bytes, tokens, steps, growth, egress, retries, notifications, reserves); activation blocked when any verified ceiling exceeded (FR-COST-009, FR-COST-006) — **FR-COST-009** **FR-COST-006**
- [ ] T043 AC-228 suite pair — `tests/acceptance/AC-228.spec.ts` + `tests/negative/AC-228.negative.spec.ts`: under simulated quota exhaustion, degradation order is social → analog → wallet-history → exploration → broad-scan depth, strictly before collector/risk/alert/mature-outcome/interactive reserve (FR-COST-003, FR-COST-004) — **FR-COST-003** **FR-COST-004**
- [ ] T044 AC-229 suite pair — `tests/acceptance/AC-229.spec.ts` + `tests/negative/AC-229.negative.spec.ts`: actual usage exceeding forecast tolerance creates an incident, recomputes admission limits, and does not silently consume paid overage or protected reserve; second over-consume within same period still incidents (FR-COST-006) — **FR-COST-006**

---

## Phase 8: Convergence + evidence

- [x] T045 Cross-artifact consistency pass per the `speckit-analyze` methodology — re-run manifest line-anchor coverage, writeScope exclusivity (three packages + `packages/domain/**` shared-vocabulary extension + `packages/shared-schemas/**` + `migrations/g0_cost_*.sql` + fixtures/AC/telemetry only), prohibited-capability scan (`node scripts/scan-prohibited-capabilities/cli.mjs`), and `pnpm verify` + `pnpm spec:verify` at the planned tree; record any findings in `specs/g0-cost-capacity/plan.md` and fix before claiming complete (FR-COST-001…010 convergence) — **FR-COST-001**
- [ ] T046 Produce the run-artifact copies — `cp specs/g0-cost-capacity/plan.md $ARTIFACTS_DIR/plan.md` and generate `$ARTIFACTS_DIR/plan-context.md` (patterns-to-mirror from tool-core/provider-lifecycle, files-to-change list, validators) so scripts/automation/package-plan-complete.mjs plus any fresh-context coding agent can execute without re-deriving context (FR-COST-001…010 artifacting) — **FR-COST-001**
