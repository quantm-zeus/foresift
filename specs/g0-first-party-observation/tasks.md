# Tasks: g0-first-party-observation

**Input**: `specs/g0-first-party-observation/spec.md` (FR-COL-001…012, FR-DISC-001…005), `specs/g0-first-party-observation/plan.md`, capsule `plan-capsule.md` bound to main `8855f1a7da61` / manifest `e0f9f1284473` / PRD `baa521d9c67e`
**Prerequisites**: plan.md (required), spec.md (required); PROVEN dependencies g0-contracts-data-truth, g0-security-perimeter, g0-provider-lifecycle, g0-cost-capacity are consumed read-only via their seams
**Tests**: Every AC has a positive AND a negative/failure suite at the manifest-declared paths `tests/acceptance/AC-*.spec.ts` / `tests/negative/AC-*.negative.spec.ts`; AC-224…229 extend the existing files additively (append-only facet model)
**Organization**: Dependency order — setup → schemas → migrations → collector-core → solana transport/registry → decoders → checkpoints/gap-recovery → discovery-universe → cheap-monitor → app composition → acceptance → convergence. Parallelizable tasks are marked `[P]`.
**Traceability**: Every line traces to at least one requirement ID or acceptance criterion of this package. No out-of-scope requirement IDs appear below.

---

## Phase 0: Setup

- [x] T001 Scaffold the seven workspace packages + app with configs — create `packages/collector-core/**`, `packages/collector-solana/**`, `packages/program-decoders/**`, `packages/collector-checkpoints/**`, `packages/collector-gap-recovery/**`, `packages/discovery-universe/**`, `packages/cheap-monitor/**`, `apps/collector/**` each with `package.json` (`@foresift/collector-core` et al., `"test": "bun test"`) and `tsconfig.json` (extends `../../tsconfig.base.json`, globs `src/**` + `test/**`); verify `pnpm install` resolves with zero root-config edits (workspace globs `packages/*` + `apps/*` already cover all paths) (FR-COL-001…012, FR-DISC-001…005 infrastructure) — **FR-COL-001**
- [ ] T002 [P] Create `tests/fixtures/col/` — versioned allowlist scopes, stream-record fixtures, reorg/duplicate/delayed/out-of-order event sequences, gap sequences with empty-range proofs, signed ProgramSupportManifest fixtures per protocol family, adversarial account layouts, capacity-ceiling declarations (FR-COL-001…010 fixtures) — **FR-COL-001**
- [ ] T003 [P] Create `tests/fixtures/disc/` — discovery-universe entries, monitor rows across §12.7 states, promotion policy versions, retrospective universe samples with/without live-lineage overlap (FR-DISC-001…005 fixtures) — **FR-DISC-001**

---

## Phase 1: Shared schemas (blocking prerequisite)

- [x] T004 Implement `packages/shared-schemas/src/col.ts` — authoritative Zod schemas (ADR-0013): `CollectorScopeDeclarationSchema`, `CollectorStreamRecordSchema` (full FR-COL-003 coordinate set), `ProgramSupportManifestSchema` (§63.3.1 interface incl. `capabilityState`/`upgradeAuthorityState`/`idlOrLayoutSha256`/`contentHash`), `CollectorPartitionStateSchema` (§12.9 vocabulary), `CollectorDecodePauseSchema`, `CollectorIncidentSchema`, `CollectorHealthSchema` (every FR-COL-008 field), `CollectorCeilingSetSchema` (eight FR-COL-010 dimensions + Sustainable Capacity Contract slice per §62.5), `FirstSeenLatencySpansSchema` (FR-COL-011), fail-closed parse helpers throwing typed errors on unknown values; import existing `@foresift/domain`/schema types rather than restating; export from `packages/shared-schemas/src/index.ts`; unit tests prove fail-closed shapes (`packages/shared-schemas/test/col.spec.ts`) (FR-COL-001…011) — **FR-COL-001** **FR-COL-003** **FR-COL-008** **FR-COL-010**
- [x] T005 [P] Implement `packages/shared-schemas/src/disc.ts` — `DiscoveryUniverseEntrySchema` (exact §63.5 interface), `DiscoverySourceClassSchema` (§63.2 vocabulary), `CheapMonitorRowSchema` (§12.7 states + finite checks/expiry/backoff/staleness), `MonitorBatchDescriptorSchema`, `CheapMonitorDecisionSchema` (REJECT_CHEAP/MONITOR_CHEAP/PROMOTE_TO_VERIFY), `PromotionDecisionSchema` (frozen feature + policy versions, inputs hash, decisionVersion), `CoveragePopulationManifestSchema` (§63.7 populations with gaps/rights exclusions/selection probabilities); export from `packages/shared-schemas/src/index.ts`; unit tests (`packages/shared-schemas/test/disc.spec.ts`) (FR-DISC-001…005) — **FR-DISC-002** **FR-DISC-005**

---

## Phase 2: Migrations

- [x] T006 Write `migrations/g0_col_0001_scopes_partitions.sql` — `col.collector_scopes` (versioned allowlist rows: chain, program, program version, event families, account filters, coverage start, finality policy, decoder version, byte/quota envelope, max lag/gap age, rights ref, retention, active) + `col.collector_partitions` (§12.9 state machine CHECK, monotonic transitions, shard/lease refs, partition→scope FK); `CREATE SCHEMA IF NOT EXISTS col`; single transaction + rollback header (FR-COL-001, FR-COL-004) — **FR-COL-001**
- [x] T007 [P] Write `migrations/g0_col_0002_stream_receipts.sql` — `col.collector_stream_receipts` (FR-COL-003 fields incl. endpoint, subscription/filter version, connection generation, slot, block hash, transaction/signature, instruction/log/account coordinates, received_at, earliest system availability, finality, raw artifact hash → object-store ref, decoder version, rights policy); `receipt_hash` UNIQUE; provenance class CHECK over §13.2 vocabulary (FR-COL-003) — **FR-COL-003**
- [x] T008 [P] Write `migrations/g0_col_0003_incidents_decodescope.sql` — `col.collector_decode_pauses` (scope = decoder/program/version, never global; reason; opening incident FK; paused_at; revalidation state) + `col.collector_incidents` (kind, affected scope, ≥1 evidence ref CHECK, audit-chain ref, resolution requires revalidation instant); decode-pause blocks derived facts via state CHECK (FR-COL-007) — **FR-COL-007**
- [x] T009 [P] Write `migrations/g0_col_0004_health_ceiling.sql` — `col.collector_health` (append-only snapshots, every FR-COL-008 field) + `col.collector_ceiling_counters` (eight dimensions, `used <= cap` CHECK, cap sourced from active Sustainable Capacity Contract slice, ceiling_exceeded_at nullable) (FR-COL-008, FR-COL-010) — **FR-COL-008** **FR-COL-010**
- [x] T010 [P] Write `migrations/g0_disc_0001_universe_entries.sql` — `disc.discovery_universe_entries` (§63.5 fields; first_ingested_at NOT NULL; unique per asset+source+sighting) + `disc.discovery_attribution` (append-only trigger: subsequent source sightings) + `disc.coverage_population_manifests` (§63.7: scope, windows, gaps, rights exclusions, program versions, selection probabilities, known missing sources) (FR-DISC-002, FR-DISC-003) — **FR-DISC-002** **FR-DISC-003**
- [x] T011 [P] Write `migrations/g0_disc_0002_cheap_monitor.sql` — `disc.cheap_monitor_rows` (§12.7 state CHECK, next_check_at, finite check counter, expiry, backoff, max staleness, budget class) + `disc.monitor_observations` (one point-in-time snapshot per returned entity per batch; batch id FK) (FR-DISC-004) — **FR-DISC-004**
- [x] T012 [P] Write `migrations/g0_disc_0003_promotions.sql` — `disc.promotion_decisions` (candidate, frozen feature versions NOT NULL, policyVersion, inputs hash UNIQUE for idempotent replay, decision, decidedAt, decisionVersion NOT NULL) (FR-DISC-005) — **FR-DISC-005**
- [ ] T013 Colocated `migrations.spec.ts` in each package — filenames, ordering, checksum pinning, idempotent apply on PGlite per ADR-0014; AND the central-registry duty (ADR-0019 plan-sanctioned scope exception): update `packages/persistence/test/migrator.spec.ts` so its expected G0 script list includes the seven `g0_col_`/`g0_disc_` scripts in lexicographic position (col_0001…0004 between cost_0004 and data_0001; disc_0001…0003 between data_0007 and dr_0001) — exact-path exception, no other `packages/persistence/**` edits (FR-COL-001…012, FR-DISC-001…005 substrate) — **FR-COL-001**

---

## Phase 3: collector-core — scope, stream records, lifecycle, capacity, health

- [x] T014 Implement `packages/collector-core/src/scope.ts` — versioned allowlist loader over `col.collector_scopes`; subscription builders express ONLY declared chains/programs/versions/accounts/event-families/finality; out-of-scope request refuses at construction with typed code; scope activation requires verified ProgramSupportManifest reference; no silent filter/version change after restart (§34.7) (FR-COL-001) — **FR-COL-001**
- [x] T015 Implement `packages/collector-core/src/stream-record.ts` — assembles FR-COL-003 records; fails closed on any missing required coordinate/timestamp/rights field; receipt hash over canonical JSON; stamps earliest system availability with §13.2 provenance class (never inferred from event time) (FR-COL-003) — **FR-COL-003**
- [x] T016 Implement `packages/collector-core/src/subscription-port.ts` + `connection-lifecycle.ts` — read-only subscription/polling port interface with fixed configured endpoints; deterministic endpoint selection (stable tie-break), bounded exponential backoff + jitter, connection-generation counters; failover replays from last committed checkpoint; fencing-token advance per generation; stale instance cannot advance after newer token takes ownership (FR-COL-009) — **FR-COL-009**
- [x] T017 Implement `packages/collector-core/src/duplicate-absorber.ts` — chain-coordinate + normalized-event-hash dedupe via `recordCanonicalEvent` (exactly-once); second receipt of same canonical key increments dedup counters, mutates nothing visible; idempotent under retry (INV-009, FR-COL-009) — **FR-COL-009** **FR-COL-006**
- [x] T018 Implement `packages/collector-core/src/capacity-governor.ts` — evaluates CPU/memory/network/subscription/event-rate/raw-storage/retry/monthly-credit counters against the active Sustainable Capacity Contract slice; ceiling reached → durable PAUSED partition + incident + safe stop before violation; resume is explicit and audited (auto-reactivation refused via the security seam); no silent paid overage or reserve consumption (FR-COL-010) — **FR-COL-010**
- [x] T019 Implement `packages/collector-core/src/health.ts` + `first-seen.ts` — health snapshot assembly covering every FR-COL-008 field (connected state, endpoint generation, head/finalized slot, checkpoint lag, gap count/duration, backfill status, decode-failure rate, streamed bytes, event rate, dedup rate, resource consumption) persisted to `col.collector_health`; first-seen span recording for collector-owned boundaries (source event → receipt → availability), later spans joinable for AC-226 decomposition; collector is independent timing reference only for verified scope (FR-COL-008, FR-COL-011) — **FR-COL-008** **FR-COL-011**
- [ ] T020 Colocated unit suites `packages/collector-core/test/**` — scope refusal matrix, stream-record fail-closed fields, backoff/generation determinism, dedupe idempotence, ceiling pause + audit, health completeness, first-seen span math, `migrations.spec.ts` (FR-COL-001…011) — **FR-COL-001** **FR-COL-008**

---

## Phase 4: collector-solana — transport, protocol registry, Jupiter observation

- [x] T021 Implement `packages/collector-solana/src/transport.ts` — persistent outbound WebSocket/RPC + bounded high-frequency polling behind the subscription port; all endpoints fixed configuration validated against `EgressGuard` COLLECTOR-plane allowlist (no event-provided URLs, §35.6); inbound message validation: chain, program/version, slot, account layout, size, decoder contract; malformed event refuses AND cannot advance the durable checkpoint (FR-COL-001, FR-COL-004, §35.6) — **FR-COL-001** **FR-COL-004**
- [x] T022 Implement `packages/collector-solana/src/program-registry.ts` — ProgramSupportManifest loading/verification: content hash, validity window, official-reference verification instant, live-chain verification slot/hash; upgraded/unsupported program without passing manifest → `UNSUPPORTED_PROGRAM_VERSION` with explicit degraded state; coverage never inherited from similar names or provider labels; mutable identifiers never copied into business logic (FR-COL-002) — **FR-COL-002**
- [x] T023 Implement `packages/collector-solana/src/endpoint-plan.ts` — deterministic subscription sharding: per-partition program/account/event-family assignment from scope declarations; stable under restart; bounded replay windows; sharding plan is a pure function of (scopes, shard count) (FR-COL-009) — **FR-COL-009**
- [x] T024 Implement `packages/collector-solana/src/jupiter-observation.ts` — Jupiter route observation/reconciliation ONLY: normalizes observed routes, reconciles to underlying venue adapters' decoded swaps, never treated as pool-math authority; module contains no quote/build/swap/sign/submit code (structural); observation-only by construction (FR-COL-002, FR-COL-012) — **FR-COL-002** **FR-COL-012**
- [ ] T025 Colocated unit suites `packages/collector-solana/test/**` — transport §35.6 validation matrix + malformed-event no-advance, egress refusal for un-allowlisted endpoint, registry manifest verification + UNSUPPORTED transitions, sharding determinism, Jupiter negative-capability assertions + venue reconciliation, `migrations.spec.ts` (FR-COL-001/002/004/009/012) — **FR-COL-002** **FR-COL-009**

---

## Phase 5: program-decoders — versioned decoders, parity, drift

- [x] T026 Implement `packages/program-decoders/src/decoder-registry.ts` — resolution strictly by (programId, programVersion, layoutHash) against signed support manifests; unknown/mismatched design returns explicit UNSUPPORTED/degraded — never generic constant-product output; partial decodes emit proven quality codes (`SCHEMA_DEGRADED`, `UNSUPPORTED_PROGRAM_VERSION`); registry is versioned and additive (FR-COL-002, AC-230 core) — **FR-COL-002**
- [x] T027 Implement family decoders `packages/program-decoders/src/decoders/pump.ts`, `raydium.ts`, `orca-whirlpools.ts`, `meteora.ts` — Pump bonding-curve lifecycle + PumpSwap migration/AMM state; Raydium AMM v4/CPMM/CLMM/Stable AMM/LaunchLab lifecycle/migration; Orca Whirlpools concentrated liquidity + tick-array state; Meteora DLMM/DAMM v1-v2/Dynamic Bonding Curve launch/migration; each emits normalized events for allowlisted families (pool/launch creation, state progress, migration, liquidity changes, authority/config changes, selected economic swaps/flows); unsupported versions remain explicit (FR-COL-002) — **FR-COL-002**
- [x] T028 Implement `packages/program-decoders/src/parity.ts` — golden vectors + valid/adversarial property/boundary harness per family; historical observed-trade parity where claimed; reference-quote parity within versioned notional-specific tolerance; upgrade-change detection (decoder hash/IDL mismatch detection); parity results recorded per decoder version (FR-COL-002, AC-231 core) — **FR-COL-002**
- [x] T029 Implement `packages/program-decoders/src/drift-detector.ts` — FR-COL-007 machinery: unknown instruction variant, account-layout change, decoder drift, or parity failure pauses ONLY the affected decode scope (writes `col.collector_decode_pauses` + incident), preserves raw events, prevents derived facts from the paused scope until explicit revalidation; unaffected scopes continue; auto-reactivation refused (FR-COL-007) — **FR-COL-007**
- [ ] T030 Colocated unit suites `packages/program-decoders/test/**` — registry mismatch → UNSUPPORTED, per-family golden + adversarial vectors, upgrade-change detection, parity tolerance boundaries, drift containment (unaffected scope keeps deriving), `migrations.spec.ts` (FR-COL-002/007) — **FR-COL-002** **FR-COL-007**

---

## Phase 6: collector-checkpoints + collector-gap-recovery — continuity

- [x] T031 Implement `packages/collector-checkpoints/src/partition-state.ts` — §12.9 machine (DISABLED/STARTING/SYNCING/LIVE/DEGRADED/GAP_DETECTED/BACKFILLING/PAUSED/FAILED) with monotonic transition table; LIVE requires connection+decoder+finality+contiguous-checkpoint+capacity+rights checks; transitions durable + audit-linked (FR-COL-004, FR-COL-008) — **FR-COL-004**
- [x] T032 Implement `packages/collector-checkpoints/src/checkpoint-store.ts` — durable monotonic per-partition checkpoints over fenced persistence helpers (`commitCheckpoint`); on reconnect compares observed ranges vs last committed checkpoint, registers every missing slot/sequence range as a gap BEFORE any backfill, resumes exactly from committed position; nothing silently skipped (FR-COL-004) — **FR-COL-004**
- [x] T033 Implement `packages/collector-checkpoints/src/watermark-coordinator.ts` — §13.5 watermark state per shard/program-version/chain; contiguous advance only through observed events or explicit empty-range proof; non-contiguous watermark blocks complete-coverage claims; unresolved gaps never erase valid observations outside the gap (§18.4, FR-COL-005 downgrade surface) — **FR-COL-004** **FR-COL-005**
- [x] T034 Implement `packages/collector-gap-recovery/src/gap-registry-bridge.ts` — §12.10 gap lifecycle over the persistence gap registry (OPEN/BACKFILL_QUEUED/BACKFILLING/RESOLVED_COMPLETE/RESOLVED_EMPTY_PROOF/PARTIAL/UNRESOLVED/WAIVED_FOR_NARROW_SCOPE); waiver scoped+signed+expiring and never supporting contiguous/full-universe claims (FR-COL-004, FR-COL-005) — **FR-COL-004** **FR-COL-005**
- [x] T035 Implement `packages/collector-gap-recovery/src/backfill-planner.ts` + `retrieval-clock.ts` — independent bounded RPC/indexer backfill where available (backfill limit, per-job bounds; operations cost-admitted via CostQuotaAdapter; `EMERGENCY_BACKFILL` reserve only per declaration-bound eligibility); every recovered event keeps original chain `event_at`, receives `available_at` = recovery fetch/commit time via `assertNoBackdating`/`recordBackfillReceipt` (persisted live receipt may prove earlier — never inferred); PARTIAL/UNRESOLVED gaps downgrade coverage + population claims explicitly (FR-COL-005) — **FR-COL-005**
- [ ] T036 Colocated unit suites `packages/collector-checkpoints/test/**` + `packages/collector-gap-recovery/test/**` — transition table exhaustiveness, fenced resume semantics, gap-before-backfill ordering, watermark contiguity rules, backfill bounds + no-backdating refusals, waiver scope/expiry, `migrations.spec.ts` (FR-COL-004/005) — **FR-COL-004** **FR-COL-005**

---

## Phase 7: discovery-universe + cheap-monitor — first-seen, registry, batches, promotion

- [x] T037 Implement `packages/discovery-universe/src/universe-registry.ts` — point-in-time registry over `disc.discovery_universe_entries` + `coverage_population_manifests`: which sources were live over which populations/windows at reference time T; supports source-overlap, unique-yield, and `NOT_DISCOVERED` miss measurement (FR-DISC-003) — **FR-DISC-003**
- [x] T038 Implement `packages/discovery-universe/src/first-seen-attribution.ts` — FR-DISC-002: first-seen records source, source timestamp, system timestamp, source rank; every subsequent discovery source appends to the attribution list (append-only); earliest valid system-available entry becomes candidate first-seen time; all entries retained for lead/lag, overlap, dependence, manipulation analysis (§63.5/§63.10) (FR-DISC-002) — **FR-DISC-002**
- [x] T039 Implement `packages/discovery-universe/src/aggregate-path.ts` — FR-DISC-001: free aggregate discovery encoded as the DEFAULT broad-universe path; discovery calls bound to the STRICT_FREE cost plane; any operation declaration not allowed in STRICT_FREE refuses (no paid operations from discovery-universe, §42.1) (FR-DISC-001) — **FR-DISC-001**
- [x] T040 Implement `packages/discovery-universe/src/retrospective-classifier.ts` — AC-111: token meeting an outcome profile but absent from all live sources classifies `NOT_DISCOVERED` through the retrospective universe path when evidence permits; classification requires disclosed lineage independence; retrospective evidence never enters the historical decision bundle (§63.9) (FR-DISC-003) — **FR-DISC-003**
- [x] T041 Implement `packages/cheap-monitor/src/monitor-store.ts` — bounded cheap-monitor rows with §12.7 states (NEW/MONITORING_CHEAP/PROMOTED_TO_VERIFY/REJECTED_CHEAP/EXPIRED_CHEAP), finite checks, expiry, backoff, maximum staleness, resource-budget class; state separate from research candidate lifecycle; expiry/rejection retained for missed-opportunity evaluation (INV-007) (FR-DISC-004) — **FR-DISC-004**
- [x] T042 Implement `packages/cheap-monitor/src/batch-scheduler.ts` — FR-DISC-004: ONE scheduled run selects rows with `next_check_at <= now` under a hard per-run bound, groups provider-compatible calls (coalescing respected), consumes first-party collector deltas first, writes one point-in-time observation per returned entity; scheduler message/workflow count is O(batches) — never one per candidate (§18.2, §63.6) (FR-DISC-004) — **FR-DISC-004**
- [x] T043 Implement `packages/cheap-monitor/src/promotion.ts` — FR-DISC-005: deterministic versioned promotion to free-quota verification; pure decision over frozen inputs (snapshots + feature versions + policy version → inputs hash → decision); replaying frozen inputs yields the identical decision record (idempotent inputs-hash unique); persistence/change + execution/security eligibility inputs, never current magnitude alone; PROMOTED_TO_VERIFY creates/advances the candidate through the versioned decision (FR-DISC-005) — **FR-DISC-005**
- [ ] T044 Colocated unit suites `packages/discovery-universe/test/**` + `packages/cheap-monitor/test/**` — PIT queries at reference instants, attribution append-only enforcement, STRICT_FREE refusal path, classification lineage rules, finite-check expiry/backoff, batch-bound invariants, promotion determinism/idempotence, `migrations.spec.ts` (FR-DISC-001…005) — **FR-DISC-004** **FR-DISC-005**

---

## Phase 8: apps/collector — composition root

- [x] T045 Implement `apps/collector/src/index.ts` + `main.ts` — durable long-running composition (NOT request-bound serverless, §10.2): boot declaration of collector_instance_id/version/chain/allowlist/filters/start checkpoint/finality policy/max lag/max gap age/backfill limit/byte+credit ceilings/backoff policy/retention+rights; pipeline: scope → transport → decoders → checkpoints → gap recovery → immutable observation append (proven repos) → first-seen + source-availability attribution → feature/update event; decode pauses and capacity pauses integrate; acquisition records created before any optional retrieval with §18.5 outcomes (RETURNED_VALID/EMPTY/PARTIAL, FAILED_*, SCHEMA_REJECTED, RIGHTS_REJECTED, COST_OR_QUOTA_BLOCKED, UNSUPPORTED) (FR-COL-001…012 wiring, FR-COL-005) — **FR-COL-001** **FR-COL-005** **FR-COL-012**
- [x] T046 Implement `apps/collector/src/leader-lease.ts` — leader lease / deterministic shard ownership; fencing-token takeover: stale instance cannot advance a checkpoint after a newer fencing token takes ownership; duplicate messages deduplicated by chain coordinates + event hash (§10.2, INV-009) (FR-COL-004, FR-COL-009) — **FR-COL-009**
- [ ] T047 Colocated suites `apps/collector/test/**` — `boot.spec.ts` (declaration completeness, no signing/key/wallet capability anywhere in the boot surface — negative assertions), `leader-lease.spec.ts` (takeover fencing), `ingest-path.spec.ts` (end-to-end over PGlite: subscription fixture → stream record → decode → checkpoint → observation → attribution; §18.4 path order) (FR-COL-001…012) — **FR-COL-012** **FR-COL-004**

---

## Phase 9: Acceptance + negative suites (13 ACs — each AC exactly two files; AC-224…229 append-only facet extension)

Header comment in every touched file states the requirement IDs it traces and the
package-owned facet ("collector facet: …"). Existing AC-224…229 files keep their
cost-facet content untouched; this package APPENDS collector-facet describe blocks
and extends the header trace lists.

- [ ] T048 AC-110 suite pair — `tests/acceptance/AC-110.spec.ts` (NEW) + `tests/negative/AC-110.negative.spec.ts` (NEW): deterministic first-seen source/system timestamps + source attribution across multiple sources; earliest valid system-available entry wins; all subsequent sources appended (FR-COL-011, FR-DISC-002) — **FR-DISC-002**
- [ ] T049 AC-111 suite pair — `tests/acceptance/AC-111.spec.ts` (NEW) + `tests/negative/AC-111.negative.spec.ts` (NEW): absent-from-live-sources token classified `NOT_DISCOVERED` via retrospective universe path when evidence permits; lineage disclosure required; retrospective evidence excluded from historical decision bundle (FR-DISC-003) — **FR-DISC-003**
- [ ] T050 AC-112 suite pair — `tests/acceptance/AC-112.spec.ts` (NEW) + `tests/negative/AC-112.negative.spec.ts` (NEW): 1,000 monitored candidates → bounded batch runs; scheduler message/workflow count O(batches); negative: per-candidate message creation structurally refused (FR-DISC-004) — **FR-DISC-004**
- [ ] T051 AC-113 suite pair — `tests/acceptance/AC-113.spec.ts` (NEW) + `tests/negative/AC-113.negative.spec.ts` (NEW): promotion replayable from frozen feature + policy versions (identical decision on replay; versioned change on policy bump); negative: unfrozen/stale inputs refuse (FR-DISC-005) — **FR-DISC-005**
- [ ] T052 AC-224 facet extension — append collector-facet blocks to `tests/acceptance/AC-224.spec.ts` + `tests/negative/AC-224.negative.spec.ts`: killed connection → reconnect from durable checkpoint; induced slot gap detected + registered BEFORE backfill; backfilled or explicitly unresolved; zero duplicate canonical events; negative: malformed event cannot advance checkpoint; stale fencing token refused; replayed window produces no second canonical row (FR-COL-004, FR-COL-005, FR-COL-006, FR-COL-009) — **FR-COL-004** **FR-COL-006**
- [ ] T053 AC-225 facet extension — append to `tests/acceptance/AC-225.spec.ts` + `tests/negative/AC-225.negative.spec.ts`: backfilled event retains original chain time; `available_at` no earlier than real retrieval; historical replay before retrieval cannot see it; negative: backdated `available_at` refused structurally; live-receipt proof without persisted ref refused (FR-COL-005) — **FR-COL-005**
- [ ] T054 AC-226 facet extension — append to `tests/acceptance/AC-226.spec.ts` + `tests/negative/AC-226.negative.spec.ts`: first-seen latency decomposition (event→collector, collector→feature, feature→decision, decision→delivery, provider comparison) for the verified collector scope; negative: spans outside verified scope cannot be labeled first-party (FR-COL-011) — **FR-COL-011**
- [ ] T055 AC-227 facet extension — append to `tests/acceptance/AC-227.spec.ts` + `tests/negative/AC-227.negative.spec.ts`: collector ceilings participate in the 30-day expected+stress replay; activation blocked when any verified collector ceiling exceeded; negative: replay with collector counters excluded refuses rather than passes (FR-COL-010) — **FR-COL-010**
- [ ] T056 AC-228 facet extension — append to `tests/acceptance/AC-228.spec.ts` + `tests/negative/AC-228.negative.spec.ts`: under simulated quota exhaustion collector continuity is preserved while social/analog/wallet-history/exploration/broad-scan depth degrade first; negative: broad-scan workload cannot route into EMERGENCY_BACKFILL reserve (declaration-bound eligibility) (FR-COL-010, FR-COL-005) — **FR-COL-010**
- [ ] T057 AC-229 facet extension — append to `tests/acceptance/AC-229.spec.ts` + `tests/negative/AC-229.negative.spec.ts`: monthly-credit usage over forecast tolerance → incident + recomputed admission limits; no silent paid overage or protected-reserve consumption; negative: second overage incidents again; pause resume without audit ref refused (FR-COL-010) — **FR-COL-010**
- [ ] T058 AC-230 suite pair — `tests/acceptance/AC-230.spec.ts` (NEW) + `tests/negative/AC-230.negative.spec.ts` (NEW): full fixture sweep — Pump/PumpSwap, Raydium AMM v4/CPMM/CLMM/Stable AMM/LaunchLab, Orca Whirlpools, Meteora DLMM/DAMM v1-v2/DBC, Jupiter route observation, constant-product, concentrated-liquidity, bin-based, bonding-curve, dynamic-fee, unknown — each resolves ONLY to its matching versioned decoder/adapter with signed support manifest; negative: unknown/mismatched design returns explicit unsupported/degraded, never generic constant-product output (FR-COL-002) — **FR-COL-002**
- [ ] T059 AC-231 suite pair — `tests/acceptance/AC-231.spec.ts` (NEW) + `tests/negative/AC-231.negative.spec.ts` (NEW): every active decoder/adapter passes official-layout + live-chain verification, deterministic vectors, valid/adversarial property + boundary tests, upgrade-change detection, historical observed-trade parity where claimed, reference-quote parity within versioned tolerance; Jupiter reconciled to venue adapters, never pool-math authority; negative: parity beyond tolerance fails the family; undetected layout change refuses activation (FR-COL-002, FR-COL-007) — **FR-COL-002** **FR-COL-007**
- [ ] T060 AC-237 suite pair — `tests/acceptance/AC-237.spec.ts` (NEW) + `tests/negative/AC-237.negative.spec.ts` (NEW): parity drift/program upgrade degrades ONLY affected scope, triggers active-candidate re-evaluation, preserves historical results, prevents new confirmed alerts until revalidated; negative: unaffected scope keeps producing derived facts (containment assertion); auto-reactivation refused (FR-COL-007) — **FR-COL-007**

---

## Phase 10: Telemetry catalogs + convergence + evidence

- [x] T061 [P] Create `telemetry/col.catalog.json` + `telemetry/disc.catalog.json` — declarative event-contract catalogs per the proven pattern (`contractStatus: DECLARATIVE_CONTRACT_ONLY`, emitter wiring deferred to G2): col events `scope_activated, stream_receipt_committed, checkpoint_advanced, gap_registered, gap_resolved, backfill_completed, decode_paused, incident_opened, health_snapshot, capacity_paused, reconnected, first_seen_recorded`; disc events `entry_recorded, attribution_appended, batch_executed, monitor_decision, promoted, monitor_expired, coverage_measured`; fields mirror the authoritative Zod schemas exactly; no secret material (FR-COL-001…012, FR-DISC-001…005 catalogs) — **FR-COL-008**
- [x] T062 Cross-artifact consistency pass per the `speckit-analyze` methodology — manifest line-anchor coverage for all 17 requirements, writeScope exclusivity (seven packages + `apps/collector/**` + `packages/shared-schemas/{col,disc}.ts` + `migrations/g0_col_*.sql`/`g0_disc_*.sql` + fixtures/AC/telemetry only, plus the named central-migration-suite exception), prohibited-capability scan (`node scripts/scan-prohibited-capabilities/cli.mjs` — collector surface must add zero findings), and `pnpm verify` + `pnpm spec:verify` at the planned tree; record findings and fix before claiming complete (FR-COL-001…012, FR-DISC-001…005 convergence) — **FR-COL-001** **FR-DISC-001**
- [ ] T063 Regenerate the coordinator test manifest (`node scripts/automation/bun-migration-manifest.mjs --out evidence/bun-migration/bun-migration-manifest.json`) after all new test files exist, so `pnpm test` sees the new suites; verify workload classification (PGlite suites → DATABASE_PGLITE) (FR-COL-001…012 verification substrate) — **FR-COL-001**
- [ ] T064 Produce the run-artifact copies — `cp specs/g0-first-party-observation/plan.md $ARTIFACTS_DIR/plan.md` and generate `$ARTIFACTS_DIR/plan-context.md` (files-to-change list, validation commands, patterns to mirror) so `scripts/automation/package-plan-complete.mjs` plus any fresh-context coding agent can execute without re-deriving context (FR-COL-001…012, FR-DISC-001…005 artifacting) — **FR-COL-001**

---

## Dependencies & execution order

- **Phases 1–2** block everything (schemas + migrations are the shared contract).
- **Phase 3** (collector-core) blocks Phases 4–6 and 8 (they consume the port,
  record, lifecycle, and governor interfaces).
- **Phases 4–7** are mutually independent ([P]-friendly across packages) once
  Phase 3 lands; Phase 6 depends additionally on Phase 2 migrations only.
- **Phase 8** composes everything; **Phase 9** acceptance suites require Phase 8
  (AC-224/225/226 facet extensions need the continuity/first-seen machinery);
  AC-230/231/237 need Phase 5; AC-110–113 need Phase 7.
- **Phase 10** is terminal (telemetry catalogs can start any time after Phase 1;
  marked [P]).

## Notes

- `packages/persistence/**`, `packages/security/**`, `packages/tool-core/**`,
  `packages/cost-router/**`, `packages/capacity-planner/**`,
  `packages/quota-forecast/**`, `packages/provider-lifecycle/**` are read-only
  seams — the ONLY sanctioned exception is the exact-path central migration
  registry update in T013 (ADR-0019 duty, named file).
- Every FR-COL-012 surface (no signing/private-key/wallet/transaction/arbitrary-
  subscription capability) is asserted both structurally (no such code exists)
  and by negative tests (T047, T058).
